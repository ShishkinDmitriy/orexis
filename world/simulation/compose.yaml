# GENERATED by `orexis-compose simulation` from the world.ttl beside it — do not edit.
#
# The roster is the ratified world. Add an agent there, regenerate, and it is deployed; there
# is no second list to keep in step. Run from this directory:
#
#   cd world/simulation && podman compose up -d
#   cd world/simulation && podman compose logs -f
#
# `compose.yaml` is the Compose Specification's own filename, so no -f is needed.
#
# Infra (MQTT, Influx, Grafana) lives in infra/compose.yaml — bring it up first; the agents here
# discover the broker from the world and will retry until it answers. There is no triplestore.
#
# Every agent here holds its own belief base in its own volume, so bringing this world up
# cannot disturb another world, and adding a world restarts nothing. Two worlds may run at once
# — but only if their DEVICES differ, since two agents on one topic both ingest.

name: orexis-simulation

# podman-compose puts every service in one pod by default, and a pod cannot combine with the
# per-service user-namespace mapping below ("--userns and --pod cannot be set together"). The
# mapping is the thing that makes an agent able to read its own credential and nothing else, so
# the pod is what gives way. Ignored by docker compose, which does not create pods at all.
x-podman:
  in_pod: false

services:

  mosquitto:
    build:
      context: ../../infra/mosquitto
      dockerfile: Containerfile
    image: orexis-mosquitto:local
    ports: ["1885:1885", "8885:8885"]
    restart: unless-stopped
    volumes:
      # generated by `orexis-mqtt simulation` from this world's wiring and its stated ports
      - ./mosquitto/orexis.conf:/etc/mosquitto/conf.d/orexis.conf:ro
      - ./mosquitto/passwd:/etc/mosquitto/passwd:ro
      - ./mosquitto/acl.conf:/etc/mosquitto/acl.conf:ro
      # Its authority is this world's own, so it trusts exactly one and never learns
      # that other worlds exist — the same rule the belief base already follows.
      - ./secrets/ca.crt:/etc/mosquitto/clients-ca.crt:ro
      - ./secrets/broker.crt:/etc/mosquitto/broker.crt:ro
      - ./secrets/broker.key:/etc/mosquitto/broker.key:ro
      # retained cadences outlive a restart: a sleeping board must still receive the interval
      # its agent set before the broker bounced
      - orexis-simulation-mosquitto:/var/lib/mosquitto

  sim-barrel1_level:
    build:
      # its own directory: it needs one file, and the repo root is excluded from image
      # contexts anyway — the agent image must not carry the simulator, nor it the agent
      context: ../../firmware/simulated-sensor
    image: orexis-simulator:local
    environment:
      SIM_SENSOR_ID: "barrel1_level"
      SIM_READING_TOPIC: "sensors/barrel1_level/reading"
      # What this board reports and where each value goes in its one message. A part that
      # reports two properties down one line is a list of two; a probe is a list of one.
      SIM_VALUES: '[{"pointer":"/value","min":0.0,"max":5.0,"initial":3.0,"litres":1.0,"drains":-1.0}]'
      # sensing:ScheduledProcedure keeps the interval its agent gives it, like a deep-sleeping board;
      # sensing:PushProcedure keeps its own clock and takes no orders. The agent derives its capability
      # from the same fact and never learns which side of it this is.
      SIM_SENSE_MODE: "push"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
      SIM_DOSE_TOPIC: "actuators/city_valve/status"
      SIM_DRAIN_TOPIC: "actuators/valve_fern/status,actuators/valve_succulent/status,actuators/valve_tomato/status"
      SIM_TICK_SECONDS: "30"
      SIM_TIMESCALE: "144"
      SIM_ALARM_PERSIST_LOOKS: "2"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a board's is — the broker has no
      # way to tell a stand-in from hardware, which is the point
      - ./secrets/mqtt-barrel1_level.env
    network_mode: host
    restart: unless-stopped

  sim-moisture_sensor_fern:
    build:
      # its own directory: it needs one file, and the repo root is excluded from image
      # contexts anyway — the agent image must not carry the simulator, nor it the agent
      context: ../../firmware/simulated-sensor
    image: orexis-simulator:local
    environment:
      SIM_SENSOR_ID: "moisture_sensor_fern"
      SIM_READING_TOPIC: "sensors/moisture_sensor_fern/reading"
      # What this board reports and where each value goes in its one message. A part that
      # reports two properties down one line is a list of two; a probe is a list of one.
      SIM_VALUES: '[{"pointer":"/moisture","min":0.0,"max":1.0,"initial":0.45,"loses":0.12,"litres":2.0},{"pointer":"/temperature","min":-10.0,"max":45.0,"initial":21.0,"swing":4.0}]'
      # sensing:ScheduledProcedure keeps the interval its agent gives it, like a deep-sleeping board;
      # sensing:PushProcedure keeps its own clock and takes no orders. The agent derives its capability
      # from the same fact and never learns which side of it this is.
      SIM_SENSE_MODE: "scheduled"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
      SIM_COMMAND_TOPIC: "sensors/moisture_sensor_fern/command"
      SIM_DOSE_TOPIC: "actuators/valve_fern/status"
      SIM_TICK_SECONDS: "3"
      SIM_TIMESCALE: "144"
      SIM_RAIN_TOPIC: "rain/fern"
      SIM_ALARM_PERSIST_LOOKS: "2"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a board's is — the broker has no
      # way to tell a stand-in from hardware, which is the point
      - ./secrets/mqtt-moisture_sensor_fern.env
    network_mode: host
    restart: unless-stopped

  sim-moisture_sensor_succulent:
    build:
      # its own directory: it needs one file, and the repo root is excluded from image
      # contexts anyway — the agent image must not carry the simulator, nor it the agent
      context: ../../firmware/simulated-sensor
    image: orexis-simulator:local
    environment:
      SIM_SENSOR_ID: "moisture_sensor_succulent"
      SIM_READING_TOPIC: "sensors/moisture_sensor_succulent/reading"
      # What this board reports and where each value goes in its one message. A part that
      # reports two properties down one line is a list of two; a probe is a list of one.
      SIM_VALUES: '[{"pointer":"/moisture","initial":0.45,"loses":0.04,"litres":2.0}]'
      # sensing:ScheduledProcedure keeps the interval its agent gives it, like a deep-sleeping board;
      # sensing:PushProcedure keeps its own clock and takes no orders. The agent derives its capability
      # from the same fact and never learns which side of it this is.
      SIM_SENSE_MODE: "scheduled"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
      SIM_COMMAND_TOPIC: "sensors/moisture_sensor_succulent/command"
      SIM_DOSE_TOPIC: "actuators/valve_succulent/status"
      SIM_TICK_SECONDS: "3"
      SIM_TIMESCALE: "144"
      SIM_RAIN_TOPIC: "rain/succulent"
      SIM_ALARM_PERSIST_LOOKS: "2"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a board's is — the broker has no
      # way to tell a stand-in from hardware, which is the point
      - ./secrets/mqtt-moisture_sensor_succulent.env
    network_mode: host
    restart: unless-stopped

  sim-moisture_sensor_tomato:
    build:
      # its own directory: it needs one file, and the repo root is excluded from image
      # contexts anyway — the agent image must not carry the simulator, nor it the agent
      context: ../../firmware/simulated-sensor
    image: orexis-simulator:local
    environment:
      SIM_SENSOR_ID: "moisture_sensor_tomato"
      SIM_READING_TOPIC: "sensors/moisture_sensor_tomato/reading"
      # What this board reports and where each value goes in its one message. A part that
      # reports two properties down one line is a list of two; a probe is a list of one.
      SIM_VALUES: '[{"pointer":"/moisture","initial":0.45,"loses":0.2,"litres":2.0}]'
      # sensing:ScheduledProcedure keeps the interval its agent gives it, like a deep-sleeping board;
      # sensing:PushProcedure keeps its own clock and takes no orders. The agent derives its capability
      # from the same fact and never learns which side of it this is.
      SIM_SENSE_MODE: "scheduled"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
      SIM_COMMAND_TOPIC: "sensors/moisture_sensor_tomato/command"
      SIM_DOSE_TOPIC: "actuators/valve_tomato/status"
      SIM_TICK_SECONDS: "3"
      SIM_TIMESCALE: "144"
      SIM_RAIN_TOPIC: "rain/tomato"
      SIM_ALARM_PERSIST_LOOKS: "2"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a board's is — the broker has no
      # way to tell a stand-in from hardware, which is the point
      - ./secrets/mqtt-moisture_sensor_tomato.env
    network_mode: host
    restart: unless-stopped

  valve-city_valve:
    build:
      # its own directory: the repo root is excluded from image contexts, and firmware must not
      # carry the agent any more than the agent carries firmware
      context: ../../firmware/simulated-valve
    image: orexis-valve:local
    environment:
      VALVE_ID: "city_valve"
      VALVE_COMMAND_TOPIC: "actuators/city_valve/command"
      VALVE_STATUS_TOPIC: "actuators/city_valve/status"
      VALVE_ML_PER_SECOND: "50"
      VALVE_MAX_DOSE_ML: "5000"
      # public halves only — it verifies, it never signs
      VALVE_HOST_PUB: "/keys/host.pub"
      VALVE_CLEARING_PUB: "/keys/clearing.pub"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a real valve's would be
      - ./secrets/mqtt-city_valve.env
    network_mode: host
    restart: unless-stopped
    volumes:
      # the PUBLIC keys, and nothing else from secrets/. A stand-in that could reach host.key
      # could sign for the society, and then verifying would be theatre.
      - ./secrets/host.pub:/keys/host.pub:ro
      - ./secrets/clearing.pub:/keys/clearing.pub:ro

  valve-valve_fern:
    build:
      # its own directory: the repo root is excluded from image contexts, and firmware must not
      # carry the agent any more than the agent carries firmware
      context: ../../firmware/simulated-valve
    image: orexis-valve:local
    environment:
      VALVE_ID: "valve_fern"
      VALVE_COMMAND_TOPIC: "actuators/valve_fern/command"
      VALVE_STATUS_TOPIC: "actuators/valve_fern/status"
      VALVE_ML_PER_SECOND: "10"
      VALVE_MAX_DOSE_ML: "1000"
      # public halves only — it verifies, it never signs
      VALVE_HOST_PUB: "/keys/host.pub"
      VALVE_CLEARING_PUB: "/keys/clearing.pub"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a real valve's would be
      - ./secrets/mqtt-valve_fern.env
    network_mode: host
    restart: unless-stopped
    volumes:
      # the PUBLIC keys, and nothing else from secrets/. A stand-in that could reach host.key
      # could sign for the society, and then verifying would be theatre.
      - ./secrets/host.pub:/keys/host.pub:ro
      - ./secrets/clearing.pub:/keys/clearing.pub:ro

  valve-valve_succulent:
    build:
      # its own directory: the repo root is excluded from image contexts, and firmware must not
      # carry the agent any more than the agent carries firmware
      context: ../../firmware/simulated-valve
    image: orexis-valve:local
    environment:
      VALVE_ID: "valve_succulent"
      VALVE_COMMAND_TOPIC: "actuators/valve_succulent/command"
      VALVE_STATUS_TOPIC: "actuators/valve_succulent/status"
      VALVE_ML_PER_SECOND: "10"
      VALVE_MAX_DOSE_ML: "1000"
      # public halves only — it verifies, it never signs
      VALVE_HOST_PUB: "/keys/host.pub"
      VALVE_CLEARING_PUB: "/keys/clearing.pub"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a real valve's would be
      - ./secrets/mqtt-valve_succulent.env
    network_mode: host
    restart: unless-stopped
    volumes:
      # the PUBLIC keys, and nothing else from secrets/. A stand-in that could reach host.key
      # could sign for the society, and then verifying would be theatre.
      - ./secrets/host.pub:/keys/host.pub:ro
      - ./secrets/clearing.pub:/keys/clearing.pub:ro

  valve-valve_tomato:
    build:
      # its own directory: the repo root is excluded from image contexts, and firmware must not
      # carry the agent any more than the agent carries firmware
      context: ../../firmware/simulated-valve
    image: orexis-valve:local
    environment:
      VALVE_ID: "valve_tomato"
      VALVE_COMMAND_TOPIC: "actuators/valve_tomato/command"
      VALVE_STATUS_TOPIC: "actuators/valve_tomato/status"
      VALVE_ML_PER_SECOND: "10"
      VALVE_MAX_DOSE_ML: "1000"
      # public halves only — it verifies, it never signs
      VALVE_HOST_PUB: "/keys/host.pub"
      VALVE_CLEARING_PUB: "/keys/clearing.pub"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
    env_file:
      # its own credential, minted by `orexis-mqtt` exactly as a real valve's would be
      - ./secrets/mqtt-valve_tomato.env
    network_mode: host
    restart: unless-stopped
    volumes:
      # the PUBLIC keys, and nothing else from secrets/. A stand-in that could reach host.key
      # could sign for the society, and then verifying would be theatre.
      - ./secrets/host.pub:/keys/host.pub:ro
      - ./secrets/clearing.pub:/keys/clearing.pub:ro

  sim-meddler:
    build:
      context: ../../firmware/simulated-meddler
    image: orexis-meddler:local
    environment:
      MEDDLER_TOPICS: '["rain/fern","rain/succulent","rain/tomato"]'
      MEDDLER_MEAN_DAYS: "2"
      MEDDLER_TIMESCALE: "144"
      MQTT_HOST: "localhost"
      MQTT_PORT: "1885"
    env_file:
      - ./secrets/mqtt-meddler.env
    network_mode: host
    restart: unless-stopped

  agent-city:
    image: orexis:local
    command: ["orexis-agent"]
    environment:
      OREXIS_AGENT_ID: "city"
      # Where to keep its belief base. A named volume, because beliefs must survive a
      # restart — otherwise every start would be a partial re-birth.
      OREXIS_STORE: "/app/state"
      # the ratified world, mounted below. The agent reads files, not a service.
      OREXIS_WORLD_DIR: "/app/world"
      # Where the three files mounted below live. The agent connects with the certificate when
      # the world states a TLS port and it holds one.
      MQTT_CERT: "/app/world/secrets/agent.crt"
      MQTT_KEY: "/app/world/secrets/agent.key"
      MQTT_CA: "/app/world/secrets/ca.crt"
    env_file:
      # where the series store is, and the org — safe for every agent to hold
      - ../../infra/.env
      # this agent's own bucket and a token that opens only it, and its own broker
      # credential. Minted by `orexis-influx` and `orexis-mqtt` from this world; mounted into
      # THIS container and no other, which is what makes the isolation structural rather
      # than a promise. Run both tools before `up`, or these files do not exist.
      - ./secrets/influx-city.env
      - ./secrets/mqtt-city.env
    network_mode: host
    # Rootless podman maps YOUR uid into the container; without this the agent lands on a
    # subuid that cannot write its own belief-base volume. Map it onto the image's user.
    userns_mode: "keep-id:uid=10001,gid=10001"
    restart: unless-stopped
    volumes:
      # its own belief base, and nobody else can name it
      - orexis-simulation-city:/app/state
      # the ratified world, at a fixed path — so the agent is told only its own id and never
      # learns that other worlds exist. Only the topology and ITS OWN opening beliefs: an
      # agent has no business reading what anyone else was authored to want.
      - ./keys.ttl:/app/world/keys.ttl:ro
      - ./world.ttl:/app/world/world.ttl:ro
      - ./beliefs/city.ttl:/app/world/beliefs/city.ttl:ro
      # Its own certificate and nothing else — the same rule as the beliefs above. The private
      # key is the whole of its identity on the bus, so a neighbour's must be unreachable.
      # ca.crt is THIS WORLD'S authority — what it needs to verify its own broker. The same
      # authority vouches for both ends, which is what makes a world self-contained.
      - ./secrets/city.crt:/app/world/secrets/agent.crt:ro
      - ./secrets/city.key:/app/world/secrets/agent.key:ro
      - ./secrets/ca.crt:/app/world/secrets/ca.crt:ro
      # it actuates, so it co-signs — these two keys and nothing else
      - ./secrets/host.key:/app/world/secrets/host.key:ro
      - ./secrets/clearing.key:/app/world/secrets/clearing.key:ro
      - ./secrets/city.sign.key:/app/world/secrets/city.sign.key:ro
      - ./secrets/city.seal.key:/app/world/secrets/city.seal.key:ro
      # The trees, mounted so a code change needs a restart rather than a rebuild — the SAME
      # three the Containerfile copies, and keeping them in step is the lesson this block
      # keeps relearning. It relearned it again when `assembly/` arrived: this block said "the
      # same TWO" while the image copied three, and a restart would have failed on a missing
      # module. `tests/test_layout.py` compares the two lists now, so the next tree cannot
      # arrive in one and not the other. capabilities/ and transports/ moved inside agent/ and their husks
      # were mounted silently; then vocabulary/ moved inside packages/ and its mount failed
      # LOUDLY — a bind whose source is gone refuses to start the container, but only at the
      # next restart, which arrived with a host logout weeks after the tree moved. A mount
      # resolves when a container is CREATED, so a stale one keeps running until the day it
      # cannot.
      - ../../assembly:/app/assembly:ro
      - ../../agent:/app/agent:ro
      - ../../packages:/app/packages:ro

  agent-fern:
    image: orexis:local
    command: ["orexis-agent"]
    environment:
      OREXIS_AGENT_ID: "fern"
      # Where to keep its belief base. A named volume, because beliefs must survive a
      # restart — otherwise every start would be a partial re-birth.
      OREXIS_STORE: "/app/state"
      # the ratified world, mounted below. The agent reads files, not a service.
      OREXIS_WORLD_DIR: "/app/world"
      # Where the three files mounted below live. The agent connects with the certificate when
      # the world states a TLS port and it holds one.
      MQTT_CERT: "/app/world/secrets/agent.crt"
      MQTT_KEY: "/app/world/secrets/agent.key"
      MQTT_CA: "/app/world/secrets/ca.crt"
    env_file:
      # where the series store is, and the org — safe for every agent to hold
      - ../../infra/.env
      # this agent's own bucket and a token that opens only it, and its own broker
      # credential. Minted by `orexis-influx` and `orexis-mqtt` from this world; mounted into
      # THIS container and no other, which is what makes the isolation structural rather
      # than a promise. Run both tools before `up`, or these files do not exist.
      - ./secrets/influx-fern.env
      - ./secrets/mqtt-fern.env
    network_mode: host
    # Rootless podman maps YOUR uid into the container; without this the agent lands on a
    # subuid that cannot write its own belief-base volume. Map it onto the image's user.
    userns_mode: "keep-id:uid=10001,gid=10001"
    restart: unless-stopped
    volumes:
      # its own belief base, and nobody else can name it
      - orexis-simulation-fern:/app/state
      # the ratified world, at a fixed path — so the agent is told only its own id and never
      # learns that other worlds exist. Only the topology and ITS OWN opening beliefs: an
      # agent has no business reading what anyone else was authored to want.
      - ./keys.ttl:/app/world/keys.ttl:ro
      - ./world.ttl:/app/world/world.ttl:ro
      - ./beliefs/fern.ttl:/app/world/beliefs/fern.ttl:ro
      # Its own certificate and nothing else — the same rule as the beliefs above. The private
      # key is the whole of its identity on the bus, so a neighbour's must be unreachable.
      # ca.crt is THIS WORLD'S authority — what it needs to verify its own broker. The same
      # authority vouches for both ends, which is what makes a world self-contained.
      - ./secrets/fern.crt:/app/world/secrets/agent.crt:ro
      - ./secrets/fern.key:/app/world/secrets/agent.key:ro
      - ./secrets/ca.crt:/app/world/secrets/ca.crt:ro
      - ./secrets/fern.sign.key:/app/world/secrets/fern.sign.key:ro
      - ./secrets/fern.seal.key:/app/world/secrets/fern.seal.key:ro
      # The trees, mounted so a code change needs a restart rather than a rebuild — the SAME
      # three the Containerfile copies, and keeping them in step is the lesson this block
      # keeps relearning. It relearned it again when `assembly/` arrived: this block said "the
      # same TWO" while the image copied three, and a restart would have failed on a missing
      # module. `tests/test_layout.py` compares the two lists now, so the next tree cannot
      # arrive in one and not the other. capabilities/ and transports/ moved inside agent/ and their husks
      # were mounted silently; then vocabulary/ moved inside packages/ and its mount failed
      # LOUDLY — a bind whose source is gone refuses to start the container, but only at the
      # next restart, which arrived with a host logout weeks after the tree moved. A mount
      # resolves when a container is CREATED, so a stale one keeps running until the day it
      # cannot.
      - ../../assembly:/app/assembly:ro
      - ../../agent:/app/agent:ro
      - ../../packages:/app/packages:ro

  agent-succulent:
    image: orexis:local
    command: ["orexis-agent"]
    environment:
      OREXIS_AGENT_ID: "succulent"
      # Where to keep its belief base. A named volume, because beliefs must survive a
      # restart — otherwise every start would be a partial re-birth.
      OREXIS_STORE: "/app/state"
      # the ratified world, mounted below. The agent reads files, not a service.
      OREXIS_WORLD_DIR: "/app/world"
      # Where the three files mounted below live. The agent connects with the certificate when
      # the world states a TLS port and it holds one.
      MQTT_CERT: "/app/world/secrets/agent.crt"
      MQTT_KEY: "/app/world/secrets/agent.key"
      MQTT_CA: "/app/world/secrets/ca.crt"
    env_file:
      # where the series store is, and the org — safe for every agent to hold
      - ../../infra/.env
      # this agent's own bucket and a token that opens only it, and its own broker
      # credential. Minted by `orexis-influx` and `orexis-mqtt` from this world; mounted into
      # THIS container and no other, which is what makes the isolation structural rather
      # than a promise. Run both tools before `up`, or these files do not exist.
      - ./secrets/influx-succulent.env
      - ./secrets/mqtt-succulent.env
    network_mode: host
    # Rootless podman maps YOUR uid into the container; without this the agent lands on a
    # subuid that cannot write its own belief-base volume. Map it onto the image's user.
    userns_mode: "keep-id:uid=10001,gid=10001"
    restart: unless-stopped
    volumes:
      # its own belief base, and nobody else can name it
      - orexis-simulation-succulent:/app/state
      # the ratified world, at a fixed path — so the agent is told only its own id and never
      # learns that other worlds exist. Only the topology and ITS OWN opening beliefs: an
      # agent has no business reading what anyone else was authored to want.
      - ./keys.ttl:/app/world/keys.ttl:ro
      - ./world.ttl:/app/world/world.ttl:ro
      - ./beliefs/succulent.ttl:/app/world/beliefs/succulent.ttl:ro
      # Its own certificate and nothing else — the same rule as the beliefs above. The private
      # key is the whole of its identity on the bus, so a neighbour's must be unreachable.
      # ca.crt is THIS WORLD'S authority — what it needs to verify its own broker. The same
      # authority vouches for both ends, which is what makes a world self-contained.
      - ./secrets/succulent.crt:/app/world/secrets/agent.crt:ro
      - ./secrets/succulent.key:/app/world/secrets/agent.key:ro
      - ./secrets/ca.crt:/app/world/secrets/ca.crt:ro
      - ./secrets/succulent.sign.key:/app/world/secrets/succulent.sign.key:ro
      - ./secrets/succulent.seal.key:/app/world/secrets/succulent.seal.key:ro
      # The trees, mounted so a code change needs a restart rather than a rebuild — the SAME
      # three the Containerfile copies, and keeping them in step is the lesson this block
      # keeps relearning. It relearned it again when `assembly/` arrived: this block said "the
      # same TWO" while the image copied three, and a restart would have failed on a missing
      # module. `tests/test_layout.py` compares the two lists now, so the next tree cannot
      # arrive in one and not the other. capabilities/ and transports/ moved inside agent/ and their husks
      # were mounted silently; then vocabulary/ moved inside packages/ and its mount failed
      # LOUDLY — a bind whose source is gone refuses to start the container, but only at the
      # next restart, which arrived with a host logout weeks after the tree moved. A mount
      # resolves when a container is CREATED, so a stale one keeps running until the day it
      # cannot.
      - ../../assembly:/app/assembly:ro
      - ../../agent:/app/agent:ro
      - ../../packages:/app/packages:ro

  agent-supplier:
    image: orexis:local
    command: ["orexis-agent"]
    environment:
      OREXIS_AGENT_ID: "supplier"
      # Where to keep its belief base. A named volume, because beliefs must survive a
      # restart — otherwise every start would be a partial re-birth.
      OREXIS_STORE: "/app/state"
      # the ratified world, mounted below. The agent reads files, not a service.
      OREXIS_WORLD_DIR: "/app/world"
      # Where the three files mounted below live. The agent connects with the certificate when
      # the world states a TLS port and it holds one.
      MQTT_CERT: "/app/world/secrets/agent.crt"
      MQTT_KEY: "/app/world/secrets/agent.key"
      MQTT_CA: "/app/world/secrets/ca.crt"
    env_file:
      # where the series store is, and the org — safe for every agent to hold
      - ../../infra/.env
      # this agent's own bucket and a token that opens only it, and its own broker
      # credential. Minted by `orexis-influx` and `orexis-mqtt` from this world; mounted into
      # THIS container and no other, which is what makes the isolation structural rather
      # than a promise. Run both tools before `up`, or these files do not exist.
      - ./secrets/influx-supplier.env
      - ./secrets/mqtt-supplier.env
    network_mode: host
    # Rootless podman maps YOUR uid into the container; without this the agent lands on a
    # subuid that cannot write its own belief-base volume. Map it onto the image's user.
    userns_mode: "keep-id:uid=10001,gid=10001"
    restart: unless-stopped
    volumes:
      # its own belief base, and nobody else can name it
      - orexis-simulation-supplier:/app/state
      # the ratified world, at a fixed path — so the agent is told only its own id and never
      # learns that other worlds exist. Only the topology and ITS OWN opening beliefs: an
      # agent has no business reading what anyone else was authored to want.
      - ./keys.ttl:/app/world/keys.ttl:ro
      - ./world.ttl:/app/world/world.ttl:ro
      - ./beliefs/supplier.ttl:/app/world/beliefs/supplier.ttl:ro
      # Its own certificate and nothing else — the same rule as the beliefs above. The private
      # key is the whole of its identity on the bus, so a neighbour's must be unreachable.
      # ca.crt is THIS WORLD'S authority — what it needs to verify its own broker. The same
      # authority vouches for both ends, which is what makes a world self-contained.
      - ./secrets/supplier.crt:/app/world/secrets/agent.crt:ro
      - ./secrets/supplier.key:/app/world/secrets/agent.key:ro
      - ./secrets/ca.crt:/app/world/secrets/ca.crt:ro
      # it actuates, so it co-signs — these two keys and nothing else
      - ./secrets/host.key:/app/world/secrets/host.key:ro
      - ./secrets/clearing.key:/app/world/secrets/clearing.key:ro
      - ./secrets/supplier.sign.key:/app/world/secrets/supplier.sign.key:ro
      - ./secrets/supplier.seal.key:/app/world/secrets/supplier.seal.key:ro
      # The trees, mounted so a code change needs a restart rather than a rebuild — the SAME
      # three the Containerfile copies, and keeping them in step is the lesson this block
      # keeps relearning. It relearned it again when `assembly/` arrived: this block said "the
      # same TWO" while the image copied three, and a restart would have failed on a missing
      # module. `tests/test_layout.py` compares the two lists now, so the next tree cannot
      # arrive in one and not the other. capabilities/ and transports/ moved inside agent/ and their husks
      # were mounted silently; then vocabulary/ moved inside packages/ and its mount failed
      # LOUDLY — a bind whose source is gone refuses to start the container, but only at the
      # next restart, which arrived with a host logout weeks after the tree moved. A mount
      # resolves when a container is CREATED, so a stale one keeps running until the day it
      # cannot.
      - ../../assembly:/app/assembly:ro
      - ../../agent:/app/agent:ro
      - ../../packages:/app/packages:ro

  agent-tomato:
    image: orexis:local
    command: ["orexis-agent"]
    environment:
      OREXIS_AGENT_ID: "tomato"
      # Where to keep its belief base. A named volume, because beliefs must survive a
      # restart — otherwise every start would be a partial re-birth.
      OREXIS_STORE: "/app/state"
      # the ratified world, mounted below. The agent reads files, not a service.
      OREXIS_WORLD_DIR: "/app/world"
      # Where the three files mounted below live. The agent connects with the certificate when
      # the world states a TLS port and it holds one.
      MQTT_CERT: "/app/world/secrets/agent.crt"
      MQTT_KEY: "/app/world/secrets/agent.key"
      MQTT_CA: "/app/world/secrets/ca.crt"
    env_file:
      # where the series store is, and the org — safe for every agent to hold
      - ../../infra/.env
      # this agent's own bucket and a token that opens only it, and its own broker
      # credential. Minted by `orexis-influx` and `orexis-mqtt` from this world; mounted into
      # THIS container and no other, which is what makes the isolation structural rather
      # than a promise. Run both tools before `up`, or these files do not exist.
      - ./secrets/influx-tomato.env
      - ./secrets/mqtt-tomato.env
    network_mode: host
    # Rootless podman maps YOUR uid into the container; without this the agent lands on a
    # subuid that cannot write its own belief-base volume. Map it onto the image's user.
    userns_mode: "keep-id:uid=10001,gid=10001"
    restart: unless-stopped
    volumes:
      # its own belief base, and nobody else can name it
      - orexis-simulation-tomato:/app/state
      # the ratified world, at a fixed path — so the agent is told only its own id and never
      # learns that other worlds exist. Only the topology and ITS OWN opening beliefs: an
      # agent has no business reading what anyone else was authored to want.
      - ./keys.ttl:/app/world/keys.ttl:ro
      - ./world.ttl:/app/world/world.ttl:ro
      - ./beliefs/tomato.ttl:/app/world/beliefs/tomato.ttl:ro
      # Its own certificate and nothing else — the same rule as the beliefs above. The private
      # key is the whole of its identity on the bus, so a neighbour's must be unreachable.
      # ca.crt is THIS WORLD'S authority — what it needs to verify its own broker. The same
      # authority vouches for both ends, which is what makes a world self-contained.
      - ./secrets/tomato.crt:/app/world/secrets/agent.crt:ro
      - ./secrets/tomato.key:/app/world/secrets/agent.key:ro
      - ./secrets/ca.crt:/app/world/secrets/ca.crt:ro
      - ./secrets/tomato.sign.key:/app/world/secrets/tomato.sign.key:ro
      - ./secrets/tomato.seal.key:/app/world/secrets/tomato.seal.key:ro
      # The trees, mounted so a code change needs a restart rather than a rebuild — the SAME
      # three the Containerfile copies, and keeping them in step is the lesson this block
      # keeps relearning. It relearned it again when `assembly/` arrived: this block said "the
      # same TWO" while the image copied three, and a restart would have failed on a missing
      # module. `tests/test_layout.py` compares the two lists now, so the next tree cannot
      # arrive in one and not the other. capabilities/ and transports/ moved inside agent/ and their husks
      # were mounted silently; then vocabulary/ moved inside packages/ and its mount failed
      # LOUDLY — a bind whose source is gone refuses to start the container, but only at the
      # next restart, which arrived with a host logout weeks after the tree moved. A mount
      # resolves when a container is CREATED, so a stale one keeps running until the day it
      # cannot.
      - ../../assembly:/app/assembly:ro
      - ../../agent:/app/agent:ro
      - ../../packages:/app/packages:ro

volumes:
  orexis-simulation-mosquitto:
  orexis-simulation-city:
  orexis-simulation-fern:
  orexis-simulation-succulent:
  orexis-simulation-supplier:
  orexis-simulation-tomato:
