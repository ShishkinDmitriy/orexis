---
type: Decision
title: The effect is one declaration, and the tolerance is a pick
status: accepted
timestamp: 2026-09-02
description: >-
  What the search plans on and what the world is held to are the same declaration, the
  step's own predicted diff — a predicted reading answered by sensing within a tolerance, a
  plain fact present or gone — and the only thing an actor adds is a tolerance, a revisable
  pick. Refused: the actor sizing its own expectation (two arithmetics for one effect, and
  the disagreement looks like a lying device); a kernel constant for how close is close
  (metFraction, one-sided and unrevisable); verification by re-simulation (an imaginarium
  at every step where a shape suffices).
---

# The effect is one declaration, and the tolerance is a pick

The sovereign's rule, 2026-09-02: *the effect we plan on and the effect we observe should be
the same, otherwise it has no sense.* Until this change they were two: the search simulated a
package's `sh:construct` and scored the world it reached, and then the actor that took the step
sized an expectation of its own — `litres / conversion`, first in the bidder, then in the
actuator, once through the rule "and subtract" — and handed the keeper a delta and a
direction. One declaration read twice, by two pieces of arithmetic that had already
disagreed once (the false-UNMET incident the effect page records), and a kernel figure,
`orexis:metFraction`, saying how much of the actor's delta counted.

Now a [step](/domain/step.md) carries what its world predicted — `progression:predicts`, the same
canonical facts the plan's signature is made of — and the keeper generates the answering shape
from that when the step is taken. A predicted READING is put to the package that declared the
observation keyed (sensing, through `orexis:answer`), which answers with an observation later
than the baseline within the tolerance of the predicted value; a PLAIN fact is the kernel's,
present for an addition and gone for a retraction, one query under a shape. The actor passes
its [tolerance](/domain/tolerance.md) and nothing else. Hanoi's seven moves and the courier's
eight drives are held to the world by the same road as a dose, with no actor at all.

# What was refused

- **The actor sizing its own expectation.** It was the arrangement in place, and it worked
  when both arithmetics agreed. The argument against it is the one already recorded: two
  sources for one number is an agent planning against one future and verifying against
  another, and the failure looks like a device lying. "Run the rule and subtract" was the
  half-step; it still left the actor as the place the prediction was restated, and left a plain
  fact — a disk on a peg — with no way to be expected at all.
- **A kernel constant for how close is close.** `metFraction` was one-sided (at least a quarter
  of the delta, any overshoot accepted) and nobody could move it. How close is a belief about
  the model's quality, which differs per package and per agent and changes with evidence — a
  pick with bounds, by the same argument as the patience and the jolt threshold.
- **Verification by re-simulation.** Checking a step by forking an imaginarium at the world as
  it is now and comparing would be correct and cost a search per step; a shape compiled once
  and re-asked on every write is what the keeper already does for every other wait.

# Seams left open

- **One shape holds one kind of change.** A step predicting both a reading and a plain fact is
  held to neither and says so; no shipped action does both, and the combination waits for one
  that does.
- **Sensing's grain as the floor** of the tolerance: no sensing term states a grain yet (#518).
- **The review rule over residuals** — settled, 2026-09-03: the keeper writes what a step
  predicted and what the world showed beside the verdict, review publishes them as
  `review:Residual`, and the market's and actuation's `review.rq` re-pick the conversion on a
  lean and the tolerance on a scatter. See [review](/domain/review.md).
- **A fact the search labelled by content** (a blank node's) cannot be stated as a triple and
  is passed over with a warning; the market's claim nodes are the case to watch.
- **Booleans and string literals** in a plain fact are told from IRIs by their shape, not by
  their datatype, since the canonical form keeps neither.
