.env
infra/.env
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/

# firmware (PlatformIO build output + WiFi secrets)
.pio/
firmware/**/include/config.h

# a world's signing keys — never commit secrets. Per world, because two worlds are two
# societies and must not be able to sign for each other.
world/*/secrets/

# generated by `agora-acl` from genesis/world.ttl — regenerate, never hand-edit
infra/fuseki/config.ttl

