"""How a raw value becomes a QUANTITY — one directory per curve, discovered by looking.

The last of the three stages a reading passes through, and the one that gives a number its
unit. Borne by a binding rather than by an agent, so a member is chosen at runtime from what the
sensor declares — see `agent/scaling.py`, and
knowledge/decisions/bytes-become-a-quantity-in-stages.md.
"""
