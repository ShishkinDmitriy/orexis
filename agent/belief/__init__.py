"""The belief package of Agent 0.2.0: beliefs, and the beliefs derived on top of them.

**ONE ACT OVER THE STORE.** A writer with something to say — a reading, a claim, a fork of the
world — writes its graph as any belief is written, and `revise` concludes what the rules the store
holds say follows from it — its revisions — into a revision graph of the source's own, to a
fixpoint and never deleting. The rules are SHACL 1.2 Inference Rules' as they stand: a rules
graph, a package's rule set, SPARQL rules by layer and order. **Any belief is accepted, and revised.** Nothing here refuses one and nothing stands
between a writer and the store: a validation, and a proposal state to serve it, were built in
this package and struck the same day. Belief revision keeps the new information; dropping
testimony over a shape is a gate wearing revision's name; a law's objection to a graph is a
revision a rule can conclude, and what to do about it is the mind's. A graph with no
catalogue row is already invisible to every reader, so a writer that means its graph never to
be seen without its conclusions writes the graph, revises it and classifies it last — a
convention two lines long, which is why it is not a concept.

**IT IS THE B OF BDI.** Planning is the desires, the wants and the plans; execution is the
intentions; this is what is believed and the process by which facts follow from facts. The
rule vocabulary is SHACL 1.2 Inference Rules', adopted as it stands with nothing of ours on it,
and the engine is the store's own.

**A FUNCTION, AND NO THREAD.** Who calls `revise` and when is the writer's and the container's,
as it is for the planner and the executor: a search would call it per fork, sensing per
reading, the pass per ground. Nothing on `main` calls it yet; it is built in isolation, on the
raw store, and priced before it is adopted.
"""
