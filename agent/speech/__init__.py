"""The speech package of Agent 0.2.0: what a peer says to this agent, and what this agent says.

**A PEER'S MESSAGE IS A SPEECH ACT, NOT AN OBSERVATION** (the-agent-stack-is-a-second-axis),
so it takes a path of its own through translation. Sensing makes a number of an instrument's
bytes; a peer's bytes are already a DOCUMENT — TriG, naming its graphs and saying in its default
graph what each one is, exactly as a world's files do — and `heard` believes it as it stands.
`said` is the same door from the other side: what this agent told a peer, it believes it told,
so a round a host opened is in the host's own beliefs as it is in its bidders'.

**WHAT A PEER MAY SAY IS STATE, AND ONLY OVER WHAT A PEER SAID.** A document heard is believed
only where every graph it holds is a kind beneath `orexis:StateGraph` — the world as somebody
reports it, never a desire, an action or a rule — and a graph of that name, where one stands,
arrived the same way. So a host saying its round again replaces the round its bidders hold, and
no peer can rewrite what this agent said, what its world asserts or what it concluded; which
PEER said a graph is not told apart, because the transport does not say who spoke — the seam
signing closes (authn-authz-capabilities). What `said` puts may replace only what this agent
said before.

**IT CONCLUDES NOTHING.** What follows from a document is a revision the rules the store holds
conclude when the container hands the graphs written to the deliberator, as it does a reading's.
"""
