"""How a device is REACHED — one directory per protocol, discovered by looking.

Not a capability, deliberately: a protocol changes nothing an agent must decide. Inside
`agent/` for the same reason capabilities are — only an agent loads a driver.
"""
