# Copy to .env and adjust. Loaded by docker-compose and by the Python app.

# --- InfluxDB ---
INFLUX_USER=admin
INFLUX_PASSWORD=admin12345
INFLUX_ORG=plant-auction
INFLUX_BUCKET=sensors
INFLUX_TOKEN=dev-token-change-me

# --- Grafana / Fuseki ---
GRAFANA_PASSWORD=admin
FUSEKI_PASSWORD=admin

# --- App endpoints (host-side; the gateway runs on the Pi, not in a container) ---
MQTT_HOST=localhost
MQTT_PORT=1883
INFLUX_URL=http://localhost:8086
FUSEKI_URL=http://localhost:3030/ds
